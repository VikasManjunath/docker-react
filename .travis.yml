sudo: required
services:
  - docker


before_install:
  - docker build -t vmanjunath/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true vmanjunath/docker-react npm run test -- --coverage
